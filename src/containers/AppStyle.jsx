import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    myPage: {
        background: "black",
        height: "100vh",
          },
        }))
    
      export default useStyles;